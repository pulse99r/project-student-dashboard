
function Cohort({student}) {
  console.log(student)
  return (
    <div className="students-list">
      {/* <p>{student.cohort.cohortCode}</p> */}
      {/* <h1>This is the Students List</h1> */}
    </div>
  );
}

export default Cohort;